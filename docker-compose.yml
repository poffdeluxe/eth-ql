version: '2'

services:
  ql:
    build: .
    volumes:
      - ./src:/app/src
    ports:
      - "3001:3001"
    depends_on:
     - ethereum
  txworker:
    build: .
    volumes:
      - ./src:/app/src
    depends_on:
     - ethereum
    command:
      node src/txWorker.js
  ethereum:
    image: harshjv/testrpc
    entrypoint: testrpc -b 5
